var now = 2018;
var models = ["Toyota","Mazda","Mercedes","Opel","Tesla"];
var years = ["2015","2014","2020","2019","2024"];


/* tüm modelleri yazdır
for (var i=0;i<models.length;i++)
    {
        console.log(models[i]);
    } 


 for(var i=0; i<models.length;i++)
     {
         console.log(models[i]+" " + years[i]+ "model ve " + now-years[i] +"yaşında bir araçtır.");
     }
*/

var model = prompt("Model gir:");
var year = prompt("Yıl gir:");

years.push(year);
models.year(model);

for(i=0; i<=year.length;i++)
    {
        console.log(year[i]);
    }
